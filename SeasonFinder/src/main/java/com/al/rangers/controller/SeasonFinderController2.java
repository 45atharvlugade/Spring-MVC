package com.al.rangers.controller;

import java.util.Map;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

import com.al.rangers.service.ISeasonFinderMgmtService;

// @Controller
// @RequestMapping("/SeasonDinder2")
public class SeasonFinderController2 {
	/*
	 * @Autowired private ISeasonFinderMgmtService service;
	 * 
	 * @RequestMapping("/") public String showHomePage() {
	 * 
	 * 
	 * return "welcome"; // logical view name welcome }
	 * 
	 * @RequestMapping("/season1") public String showSeason(Map<String,Object> map)
	 * { // use service String msg=service.findSeason(); map.put("seasonName", msg);
	 * // return logical view name. return "show_result"; }
	 */

}
